# Example 4

data_type: real

nr_hidden_states: 4  # can be overridden with --extra_L argument

dataset:
  filepath: data/amerigeoss/pres_diff.csv

  load_args:
    header: 0
    compression:

  column: Diff

nr_observations: 40000  # (n in paper)
grid_size: 30           # (m in paper)
grid_strategy: "uniform"